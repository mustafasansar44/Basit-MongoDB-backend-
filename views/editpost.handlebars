<h1>Post Düzenle</h1>
<hr>

<form action="/setting/edit/add/{{data._id}}" method="POST" enctype="multipart/form-data">
    <input type="text" name="title" placeholder="Title" value="{{data.title}}">
    <textarea name="description">{{data.description}}</textarea>
    <input name="image" type="file"/><br>
    {{#if data.active}}
    <input type="radio" name="active" value="true" checked>
    <label for="active">Aktif</label>
    <input type="radio" name="active" value="false">
    <label for="passive">Pasif</label><br>
    {{else}}
    <input type="radio" name="active" value="true">
    <label for="active">Aktif</label>
    <input type="radio" name="active" value="false" checked>
    <label for="passive">Pasif</label><br>
    {{/if}}
    <button class="button">Ekle</button>
    <br><br>

</form>